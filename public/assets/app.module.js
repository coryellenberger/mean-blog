/**
 * Flotilla Angular App which will pull in any external dependencies such as 'ngRoute', 'ngResource', 'lodash'
 * @namespace flotilla
 */
(function () {
  angular
    .module('flotilla', [
      'ngRoute',
      'ngResource',
      'lodash'
    ])
})()
